{
  "private": true,
  "license": "ISC",
  "scripts": {
    "build": "vite build",
    "dev": "vite --port 3000",
    "start": "concurrently \"npm run dev\" \"webxdc-dev run http://localhost:3000\"",
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^8.1.0",
    "prettier": "^3.0.3",
    "vite": "^4.3.2",
    "webxdc-dev": "^0.17.0",
    "webxdc-vite-plugins": "github:webxdc/webxdc-vite-plugins"
  },
  "dependencies": {
    "webxdc-scores": "github:webxdc/webxdc-scores"
  }
}
